
import 'package:flutter/material.dart';

class SignSelectorScreen extends StatelessWidget {
  static const routeName = "/sign_selector_screen";
  const SignSelectorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: [

          ],
        ),
      ),
    );
  }
}
