import 'package:flutter/material.dart';


class HoroScopeScreen extends StatelessWidget {
  static const routeName = "/horoscope_screen";
  const HoroScopeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
